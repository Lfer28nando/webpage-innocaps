---
import { technologies } from '../data/sectionsData';
---

<section class="relative bg-slate-900 py-16 md:py-32 overflow-hidden">
  <!-- Fondo abstracto -->
  <div class="absolute inset-0 opacity-5">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 2px 2px, rgba(20, 184, 166, 0.3) 1px, transparent 0); background-size: 40px 40px;"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10">
    <!-- Header -->
    <div class="text-center mb-12 md:mb-20">
      <h2 class="text-3xl md:text-5xl lg:text-6xl font-bold text-white mb-4 md:mb-6">
        Espectro <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-400">Tecnológico</span>
      </h2>
      <p class="text-base md:text-xl text-slate-400 max-w-3xl mx-auto leading-[1.5]">
        Dominamos múltiples plataformas de encapsulación molecular para cada aplicación específica
      </p>
    </div>

    <!-- Grid de Tecnologías -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {technologies.map((tech, index) => (
        <div 
          class="tech-card group relative p-5 md:p-8 rounded-2xl overflow-hidden cursor-pointer transition-all duration-500 hover:scale-105 active:scale-[1.02]"
          style={`animation-delay: ${index * 100}ms`}
        >
          <!-- Glassmorphism Background -->
          <div class="absolute inset-0 bg-gradient-to-br from-slate-800/40 to-slate-900/40 backdrop-blur-xl"></div>
          <div class="absolute inset-0 bg-gradient-to-br from-teal-500/0 to-cyan-500/0 group-hover:from-teal-500/10 group-hover:to-cyan-500/10 transition-all duration-500"></div>
          
          <!-- Borde sutil -->
          <div class="absolute inset-0 rounded-2xl border border-slate-700/50 group-hover:border-teal-500/50 transition-all duration-500"></div>
          
          <!-- Glow interno en hover -->
          <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500">
            <div class="absolute top-0 left-1/2 -translate-x-1/2 w-32 h-32 bg-teal-500/20 rounded-full blur-3xl"></div>
          </div>

          <!-- Contenido -->
          <div class="relative z-10 space-y-6">
            <!-- Ícono -->
            <div class="w-16 h-16 bg-gradient-to-br from-teal-500/20 to-cyan-500/20 rounded-xl flex items-center justify-center text-4xl group-hover:scale-110 transition-transform duration-500 border border-teal-500/30">
              {tech.icon}
            </div>

            <!-- Título -->
            <h3 class="text-2xl font-bold text-white group-hover:text-teal-300 transition-colors duration-300">
              {tech.name}
            </h3>

            <!-- Descripción técnica -->
            <p class="text-slate-400 text-sm leading-relaxed">
              {tech.description}
            </p>

            <!-- Separador -->
            <div class="h-px bg-gradient-to-r from-transparent via-teal-500/30 to-transparent"></div>

            <!-- Aplicación -->
            <div class="flex items-start gap-2">
              <svg class="w-5 h-5 text-teal-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
              <p class="text-sm text-cyan-300 font-medium">
                {tech.application}
              </p>
            </div>
          </div>

          <!-- Partícula decorativa flotante -->
          <div class="absolute -top-4 -right-4 w-24 h-24 bg-teal-500/10 rounded-full blur-2xl group-hover:bg-teal-500/20 transition-all duration-700"></div>
        </div>
      ))}
    </div>

    <!-- CTA Section -->
    <div class="mt-12 md:mt-20 text-center">
      <p class="text-slate-400 mb-6 text-sm md:text-base">¿Necesitas una solución de encapsulación personalizada?</p>
      <button class="w-full md:w-auto px-8 py-4 bg-gradient-to-r from-teal-500 to-cyan-500 hover:from-teal-400 hover:to-cyan-400 active:from-teal-400 active:to-cyan-400 text-slate-900 font-bold rounded-full transition-all duration-300 shadow-lg shadow-teal-500/30 hover:shadow-teal-500/50 hover:scale-105">
        Consulta con Nuestros Expertos
      </button>
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .tech-card {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
  }

  @media (prefers-reduced-motion: no-preference) {
    .tech-card:nth-child(1) { animation-delay: 0ms; }
    .tech-card:nth-child(2) { animation-delay: 100ms; }
    .tech-card:nth-child(3) { animation-delay: 200ms; }
    .tech-card:nth-child(4) { animation-delay: 300ms; }
  }
</style>
